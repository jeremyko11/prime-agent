[project]
name = "prime-agent-skill-research-guard"
version = "0.1.0"
description = "Research guardrails: token trimming, budget caps, append-only run logs."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/research_guard"]
