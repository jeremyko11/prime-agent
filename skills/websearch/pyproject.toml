[project]
name = "prime-agent-skill-websearch"
version = "0.1.0"
description = "Web search skill for Prime Agent (DuckDuckGo, no API key needed)."
requires-python = ">=3.10"
dependencies = ["httpx>=0.27,<1", "prime-agent-runtime", "ddgs>=9.0"]

[project.scripts]
websearch = "rlm.skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/websearch"]
