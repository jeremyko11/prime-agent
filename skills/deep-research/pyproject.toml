[project]
name = "prime-agent-skill-deep-research"
version = "0.1.0"
description = "Iterative deep research loop with cross-validation and adversarial pass."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/deep_research"]
