[project]
name = "prime-agent-skill-parallel-explore"
version = "0.1.0"
description = "Parallel multi-query search / multi-URL reading with adversarial mode."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/parallel_explore"]
